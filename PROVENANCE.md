# M15 preview provenance

This preview uses original, local semantic HTML and CSS only. No third-party block, template, component code, imagery, font file, runtime dependency, telemetry, or external network request has been copied or added.

The structure follows the locally documented selected quiet centered-product shell (`docs/R05-COMMON-SHELL-SELECTION-2026-08-30.md`) and maps the frozen M10 token values from `docs/tokens/lunowa-m10.tokens.json` into CSS custom properties in `styles.css`.

This is an M15 browser preview, not production implementation and not ACP G7 acceptance.
